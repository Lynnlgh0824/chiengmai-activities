# 清迈指南 - 快速测试指南

## 一键运行所有测试

```bash
# 确保服务运行中
npm run dev

# 在新终端运行所有测试
npm run test:all
```

## 测试类型

### 1️⃣ 单元测试（15 个测试）
```bash
npm test                # 交互式模式
npm run test:run        # 命令行运行
npm run test:ui         # 可视化界面
```

### 2️⃣ API 集成测试（28 个测试）
```bash
npm run test:run -- src/test/comprehensive-api.test.js
```

**覆盖的 API**:
- ✅ 健康检查
- ✅ 活动管理（CRUD + 筛选 + 分页 + 排序）
- ✅ 后台管理（CRUD）
- ✅ 统计 API
- ✅ 飞书集成
- ✅ CORS 和安全性

### 3️⃣ E2E 端到端测试
```bash
npm run test:e2e        # 运行 E2E 测试
npm run test:e2e:ui     # 可视化模式
npm run test:e2e:debug  # 调试模式
```

**测试覆盖**:
- 🏠 **主页**: 加载、搜索、筛选、详情、响应式布局
- ⚙️ **后台**: 新增、编辑、删除活动，表单验证，图片上传

## 综合测试脚本

```bash
# 运行所有测试（推荐）
./test-all-e2e.sh all

# 单独运行
./test-all-e2e.sh unit      # 单元测试
./test-all-e2e.sh api       # API 测试
./test-all-e2e.sh e2e       # E2E 测试
./test-all-e2e.sh coverage  # 覆盖率报告
```

## 测试报告

- **单元测试**: `npm run test:ui` - 打开可视化界面
- **API 测试**: 命令行输出结果
- **E2E 测试**: `npx playwright show-report` - HTML 报告
- **覆盖率**: `npm run test:coverage` - 查看 `coverage/index.html`

## 常见问题

**Q: 测试失败提示服务器未运行？**
```bash
# 启动服务
npm run dev
```

**Q: E2E 测试超时？**
```bash
# 确保前端在 http://localhost:5173
# 确保后端在 http://localhost:3000
npm run dev
```

**Q: 如何查看详细的测试报告？**
```bash
# E2E 测试报告
npx playwright show-report

# 覆盖率报告
open coverage/index.html  # macOS
```

## 测试数据

所有测试使用的测试数据会在测试结束后自动清理：
- 创建的测试活动会自动删除
- 不会影响生产数据

## 下一步

- 📖 查看完整文档: [docs/AUTOMATED_TESTING_GUIDE.md](docs/AUTOMATED_TESTING_GUIDE.md)
- 🔧 添加新测试: 参考现有测试文件
- 📊 查看覆盖率: `npm run test:coverage`

---

**提示**: 首次运行 E2E 测试前，确保已安装 Playwright 浏览器：
```bash
npx playwright install
```
