# 🔔 Chiengmai 项目每日检查使用指南

> 三种方式任您选择，确保系统健康运行

---

## 🎯 推荐方式：终端提醒（最简单）

### 使用方法

```bash
cd /Users/yuzhoudeshengyin/Documents/my_project/Chiengmai
./show-notification.sh
```

### 效果

在终端显示菜单：
```
=========================================
🔔 Chiengmai 系统检查提醒
=========================================

请选择操作：
  1) 立即检查系统
  2) 稍后提醒
  3) 取消

请输入选项 (1-3):
```

### 优点

- ✅ 不会漏看（在终端中）
- ✅ 操作简单（输入数字即可）
- ✅ 立即反馈

### 设置为每天提醒

在终端添加别名（添加到 `~/.zshrc`）：

```bash
echo 'alias chiangmai-check="cd /Users/yuzhoudeshengyin/Documents/my_project/Chiengmai && ./show-notification.sh"' >> ~/.zshrc
source ~/.zshrc
```

以后只需运行：`chiangmai-check`

---

## 🖥️ 方式2：图形弹窗（macOS 对话框）

### 使用方法

```bash
cd /Users/yuzhoudeshengyin/Documents/my_project/Chiengmai
./test-popup.sh
```

### 效果

显示 macOS 对话框，带有按钮：
- 立即检查
- 取消

### 注意事项

⚠️ 如果弹窗被其他窗口遮挡，请：
1. 查看 Dock 栏，点击"脚本编辑器"图标
2. 或使用 `Cmd + Tab` 切换应用

### 设置为每天自动提醒

```bash
# 安装定时任务
./install-daily-check.sh

# 每天10:00会自动弹出对话框
```

---

## 📱 方式3：通知中心（静默提醒）

### 使用方法

直接发送通知，不打断工作：

```bash
cd /Users/yuzhoudeshengyin/Documents/my_project/Chiengmai
osascript -e 'display notification "🔔 记得检查 Chiengmai 系统健康状态" with title "Chiengmai 每日提醒"'
```

### 效果

在右上角通知中心显示提醒。

### 手动检查

```bash
# 快速检查
./quick-check.sh

# 或直接运行测试
python3 test-enhanced-fixed.sh
```

---

## 🎨 快速测试脚本说明

| 脚本 | 用途 | 运行方式 |
|------|------|---------|
| `show-notification.sh` | **终端菜单提醒** | `./show-notification.sh` |
| `test-popup.sh` | 图形弹窗提醒 | `./test-popup.sh` |
| `quick-check.sh` | 快速检查（无提醒） | `./quick-check.sh` |
| `test-enhanced-fixed.sh` | 完整系统测试 | `python3 test-enhanced-fixed.sh` |

---

## 💡 推荐使用流程

### 日常使用（推荐）

```bash
# 每天打开电脑后运行
cd /Users/yuzhoudeshengyin/Documents/my_project/Chiengmai
./show-notification.sh

# 选择 1) 立即检查系统
# 查看测试结果
```

### 修改代码后

```bash
# 每次修改代码后
python3 test-enhanced-fixed.sh

# 确认所有测试通过
# 继续开发
```

### 想要自动化

```bash
# 安装定时任务
./install-daily-check.sh

# 每天10:00自动提醒
# 可以选择立即检查或稍后提醒
```

---

## 🔧 快速设置

### 1. 创建便捷命令（推荐）

```bash
# 添加到 ~/.zshrc
cat >> ~/.zshrc << 'END'

# Chiengmai 项目快捷命令
alias cc-check='cd /Users/yuzhoudeshengyin/Documents/my_project/Chiengmai && ./show-notification.sh'
alias cc-test='cd /Users/yuzhoudeshengyin/Documents/my_project/Chiengmai && python3 test-enhanced-fixed.sh'
alias cc-quick='cd /Users/yuzhoudeshengyin/Documents/my_project/Chiengmai && ./quick-check.sh'
END

# 重新加载配置
source ~/.zshrc
```

以后使用：
- `cc-check` - 每日检查
- `cc-test` - 完整测试
- `cc-quick` - 快速检查

### 2. 设置每天自动提醒

```bash
cd /Users/yuzhoudeshengyin/Documents/my_project/Chiengmai
./install-daily-check.sh
```

### 3. 验证安装

```bash
# 测试弹窗
./test-popup.sh

# 测试终端提醒
./show-notification.sh
```

---

## 📊 测试结果说明

### 全部通过 ✅

```
🎉 所有测试通过！系统运行正常！
✅ 主页功能正常
✅ 日历页面功能正常
✅ 管理后台功能正常
✅ API端点正常
✅ 数据一致性良好
```

### 有失败时 ⚠️

```
⚠️ 有 X 个测试失败

💡 修复建议:
• 具体问题
  建议: 解决方案
```

---

## 🆘 常见问题

### Q: 弹窗没有显示怎么办？

**A**: 使用终端版提醒：
```bash
./show-notification.sh
```

### Q: 如何禁用定时提醒？

**A**:
```bash
launchctl unload ~/Library/LaunchAgents/com.chiangmai.dailycheck.plist
```

### Q: 测试失败了怎么办？

**A**:
1. 查看日志：`cat logs/daily-check-$(date +%Y%m%d).log`
2. 检查服务器：`lsof -i :5173` 和 `lsof -i :3000`
3. 重启服务：`npm run dev`

### Q: 如何自定义提醒时间？

**A**: 编辑 `com.chiangmai.dailycheck.plist`，修改时间后重新加载。

---

## 📞 获取帮助

- 📖 查看详细文档：`docs/定时检查系统设置.md`
- 🧪 运行测试：`python3 test-enhanced-fixed.sh`
- 📋 查看日志：`cat logs/daily-check-*.log`

---

**最后更新**：2025-01-25
**版本**：1.0
